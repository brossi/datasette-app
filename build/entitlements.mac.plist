<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!--
      Hardened-runtime entitlements. This app is NOT App-Sandboxed.

      The first four are Electron's standard hardened-runtime set, required so
      V8's JIT and the bundled standalone Python (which loads unsigned,
      third-party C-extension dylibs) run under the hardened runtime.
    -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!--
      Network: the app downloads packages from PyPI / checks for updates, and
      runs a local Datasette HTTP server it connects to (optionally exposed on
      the LAN via File -> Access Control).
    -->
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <!--
      Automation (sending Apple Events to other apps). Kept pending confirmation
      that it is actually used; a signed build that works without it should drop
      this. Removed previously: com.apple.security.cs.debugger (debugging other
      processes, unused in production) and the App-Sandbox entitlements
      files.user-selected.read-only and inherit (inert without App Sandbox).
    -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
  </dict>
</plist>
